console.log("Source Intelligence Engine: Initialized");
document.addEventListener('DOMContentLoaded', () => {
    // Dynamic Node Simulation
    const canvas = document.querySelector('.interactive-node-canvas');
    if(canvas) {
        canvas.innerHTML = '<div class="node-pulse"></div>';
    }
});
